{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + shell (sidecar spawn) permissions",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
